version: 1
sequences:
  - protein:
      id: [A, B, C, D] # four identical alpha chains
      sequence: {{ alpha_seq }}
{%- if msa_file %}
      msa: {{ msa_file }}
{%- endif %}
  - protein:
      id: [E, F, G, H] # four identical beta chains
      sequence: {{ beta_seq }}
{%- if msa_file %}
      msa: {{ msa_file }}
{%- endif %}
  - ligand:
      id: I
      smiles: '{{ smiles }}'
templates:
  - cif: {{ cif_file }}
